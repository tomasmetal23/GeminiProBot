services:
  gemma_bot:
    image: ghcr.io/tomasmetal23/gemmaprobot:latest
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./data:/app/data       # SQLite persistent memory
      - ./session:/app/session # Pyrogram session files
    restart: unless-stopped
