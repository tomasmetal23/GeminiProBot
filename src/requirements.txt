# Telegram Bot Framework
pyrogram[fast]
tgcrypto

# Para análisis de imágenes (Base64)
pillow

# Conexión local a LM Studio (protocolo OpenAI-compatible)
openai

# Búsqueda web sin API key
duckduckgo-search

# Extracción de texto de PDFs
pdfplumber

# Para cargar variables de entorno en desarrollo local (opcional)
python-dotenv
