# Archivos de entorno y secretos (NUNCA subir .env real a git)
.env
# .env.example SÍ se sube — contiene valores de ejemplo, no secretos reales

# Entornos virtuales de Python
bot-env/
venv/
__pycache__/

# Archivos de sesión de Pyrogram
*.session
*.session-journal
session/

# Base de datos local
data/